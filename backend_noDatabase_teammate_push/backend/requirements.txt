fastapi>=0.115
uvicorn>=0.30
pytest>=8
httpx>=0.27
pymongo[srv]>=4.18
python-dotenv>=1.0

# test only: an in-memory stand-in for mongo, so the repository tests run
# without a cluster or a network connection
mongomock>=4.3
